[project]
name = "simple-otp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ruslan Iskov", email = "ruslan.rv.ua+github@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "cryptography>=46.0.2",
    "objectlistview3>=1.3.5",
    "pyotp>=2.9.0",
    "wxpython>=4.2.3",
]

[project.scripts]
simple-otp = "simple_otp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
